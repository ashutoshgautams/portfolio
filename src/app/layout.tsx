import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Ashutosh Gautam - Interactive Portfolio & Developer Playground',
  description: 'Visualize and interact with code in 3D space. Build your portfolio and experiment with JavaScript in a unique 3D environment.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
